/**
 * Motion disabled for instant text — content is visible on first paint.
 * Keep file so existing script tags do not 404.
 */
(function () {
  "use strict";
  document.documentElement.classList.add("page-loaded");
})();
